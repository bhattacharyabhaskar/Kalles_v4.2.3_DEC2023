{% for product in collections.all.products %}
  <h2>Checking: {{ product.title }}</h2>
  
  {% if product.available and product.total_inventory > 0 %}
    <p>✅ Product is available (Inventory: {{ product.total_inventory }})</p>
    
    {% if product.media != blank %}
      <p>🎥 Media Found:</p>
      <ul>
        {% for media in product.media %}
          <li>Type: {{ media.media_type }} - Source: {% if media.sources != blank %} ✅ Exists {% else %} ❌ Missing {% endif %}</li>
        {% endfor %}
      </ul>
    {% else %}
      <p>❌ No media found for this product.</p>
    {% endif %}
  
  {% else %}
    <p>❌ Product is out of stock.</p>
  {% endif %}
{% endfor %}
